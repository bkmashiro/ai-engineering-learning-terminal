<figure class="system-stack">
  <div
    class="system-stack__frame"
    role="group"
    aria-labelledby="system-stack-title"
    aria-describedby="system-stack-description"
  >
    <header class="system-stack__header">
      <span class="system-stack__heading">SYSTEM VIEW</span>
      <strong id="system-stack-title">从需求走到可验证运行</strong>
    </header>

    <div class="system-stack__body">
      <ol class="system-stack__layers" aria-label="AI工程系统分层">
        <li style="--stack-inset: 0rem">
          <span class="system-stack__index">01</span>
          <strong>用户任务与交互</strong>
          <span class="system-stack__scope">输入 / 反馈</span>
        </li>
        <li style="--stack-inset: .65rem">
          <span class="system-stack__index">02</span>
          <strong>Agent策略与工作流</strong>
          <span class="system-stack__scope">决策 / 边界</span>
        </li>
        <li style="--stack-inset: 1.3rem">
          <span class="system-stack__index">03</span>
          <strong>模型、上下文与检索</strong>
          <span class="system-stack__scope">Token / 证据</span>
        </li>
        <li style="--stack-inset: 1.95rem">
          <span class="system-stack__index">04</span>
          <strong>工具与协议</strong>
          <span class="system-stack__scope">Schema / 权限</span>
        </li>
        <li style="--stack-inset: 2.6rem">
          <span class="system-stack__index">05</span>
          <strong>状态、运行时与队列</strong>
          <span class="system-stack__scope">取消 / 重试</span>
        </li>
        <li style="--stack-inset: 3.25rem">
          <span class="system-stack__index">06</span>
          <strong>存储、网络与基础设施</strong>
          <span class="system-stack__scope">持久化 / 部署</span>
        </li>
      </ol>

      <aside class="system-stack__rail" aria-label="贯穿所有层的验证能力">
        <span>EVIDENCE</span>
        <strong>评测 · 安全 · TRACE</strong>
        <span>REPLAY</span>
      </aside>
    </div>
  </div>

  <figcaption id="system-stack-description">
    课程不是从“调一个模型接口”开始，而是从输入、状态、边界和证据开始。
  </figcaption>
</figure>

<style>
  .system-stack {
    margin: 1.2rem 0 2rem;
  }

  .system-stack__frame {
    padding: clamp(1rem, 3vw, 1.6rem);
    border: 1px solid var(--sl-color-gray-5);
    border-radius: 1rem;
    background: var(--sl-color-gray-7);
  }

  .system-stack__header {
    display: flex;
    align-items: baseline;
    gap: .7rem;
    margin-bottom: 1rem;
  }

  .system-stack__heading {
    color: var(--sl-color-accent-high);
    font-family: var(--sl-font-mono);
    font-size: 1rem;
    font-weight: 800;
    letter-spacing: .04em;
  }

  .system-stack__header strong {
    color: var(--sl-color-text);
    font-size: 1rem;
    font-weight: 700;
  }

  .system-stack__body {
    display: grid;
    grid-template-columns: minmax(0, 1fr) 8.5rem;
    gap: 1rem;
  }

  .system-stack__layers {
    display: grid;
    gap: .55rem;
    margin: 0;
    padding: 0;
    list-style: none;
  }

  .system-stack__layers li {
    display: grid;
    grid-template-columns: 2rem minmax(0, 1fr) auto;
    align-items: center;
    gap: .75rem;
    min-height: 3.25rem;
    margin-left: var(--stack-inset);
    padding: .7rem .9rem;
    border: 1px solid var(--sl-color-gray-5);
    border-radius: .65rem;
    background: var(--sl-color-bg);
  }

  .system-stack__layers li:nth-child(even) {
    background: color-mix(in srgb, var(--sl-color-accent) 5%, var(--sl-color-bg));
  }

  .system-stack__index {
    color: var(--sl-color-accent-high);
    font-family: var(--sl-font-mono);
    font-size: .85rem;
    font-weight: 800;
  }

  .system-stack__layers strong {
    color: var(--sl-color-text);
    font-size: 1rem;
    line-height: 1.35;
  }

  .system-stack__scope {
    color: var(--sl-color-gray-2);
    font-family: var(--sl-font-mono);
    font-size: .9rem;
    font-weight: 650;
    white-space: nowrap;
  }

  .system-stack__rail {
    display: flex;
    min-width: 0;
    flex-direction: column;
    align-items: center;
    justify-content: space-between;
    gap: 1rem;
    padding: 1rem .75rem;
    border: 1.5px dashed #16845c;
    border-radius: .8rem;
    color: color-mix(in srgb, #16845c 78%, var(--sl-color-text));
    background: color-mix(in srgb, #16845c 9%, var(--sl-color-bg));
    text-align: center;
  }

  .system-stack__rail span {
    font-family: var(--sl-font-mono);
    font-size: .85rem;
    font-weight: 800;
    letter-spacing: .04em;
  }

  .system-stack__rail strong {
    max-width: 6.5rem;
    font-size: 1rem;
    line-height: 1.7;
  }

  .system-stack figcaption {
    margin-top: .7rem;
    color: var(--sl-color-gray-2);
    font-size: .9rem;
    text-align: center;
  }

  @media (max-width: 42rem) {
    .system-stack__header {
      display: grid;
      gap: .15rem;
    }

    .system-stack__body {
      grid-template-columns: 1fr;
    }

    .system-stack__layers li {
      grid-template-columns: 1.8rem minmax(0, 1fr);
      margin-left: 0;
    }

    .system-stack__scope {
      grid-column: 2;
      white-space: normal;
    }

    .system-stack__rail {
      flex-direction: row;
      text-align: left;
    }

    .system-stack__rail strong {
      max-width: none;
      line-height: 1.4;
    }
  }
</style>
